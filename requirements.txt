# --- 共通 ---
PyYAML>=6.0
python-dotenv>=1.0

# --- Warehouse: BigQuery（本番） / DuckDB（ローカル検証） ---
google-cloud-bigquery>=3.25
duckdb>=1.0

# --- App DB: Neon / PostgreSQL ---
psycopg[binary]>=3.2

# --- 既存FW共有シートの読み取り ---
google-api-python-client>=2.140
google-auth>=2.33

# --- Object Storage: Cloudflare R2（S3互換） ---
boto3>=1.34

# --- FW のブラウザ操作（既存パイプラインと同じバージョンに揃える）---
playwright==1.49.1

# --- 開発 ---
pytest>=8.0
